version: "3.8"

services:
  flask_app:
    build: .
    ports:
      - "5000:5000"
    container_name: flask_container
#gebruik deze commando om image en container te runnen: docker-compose up --build -d